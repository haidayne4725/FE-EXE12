import { Edit3, Plus, Trash2 } from "lucide-react";
import { useEffect, useState, type FormEvent } from "react";
import { Loading } from "../../components/Loading";
import { deleteProduct, getCategories, getProducts, saveProduct } from "../../services/catalogService";
import { getApiMessage } from "../../services/axiosClient";
import type { Category, Product } from "../../types/catalog";
import { formatMoney } from "../../utils/format";
import { AdminHead } from "./AdminDashboard";

const empty = { name: "", slug: "", basePrice: 0, salePrice: "", inventory: 0, categoryId: "", status: "ACTIVE", description: "", setupStatus: "READY_TO_DISPLAY", careLevel: "EASY", image: "/home-hero-full-banner.png", watering: "Kiểm tra ẩm mỗi 3 ngày" };
export function AdminProducts() { const [products, setProducts] = useState<Product[] | null>(null); const [categories, setCategories] = useState<Category[]>([]); const [editing, setEditing] = useState<Product | null>(null); const [form, setForm] = useState(empty); const [open, setOpen] = useState(false); const [message, setMessage] = useState(""); const load = () => getProducts({ status: "all", limit: 100 }).then((page) => setProducts(page.products)); useEffect(() => { load(); getCategories(true).then(setCategories); }, []); function start(product?: Product) { setEditing(product || null); setForm(product ? { name: product.name, slug: product.slug, basePrice: product.basePrice, salePrice: product.salePrice ? String(product.salePrice) : "", inventory: product.inventory, categoryId: product.categoryId, status: product.status, description: product.description || "", setupStatus: product.setupStatus || "READY_TO_DISPLAY", careLevel: product.careLevel || "EASY", image: product.images[0] || "/logo.png", watering: product.careGuide?.watering || "Kiểm tra ẩm mỗi 3 ngày" } : { ...empty, categoryId: categories[0]?.id || "" }); setOpen(true); } async function submit(event: FormEvent) { event.preventDefault(); try { await saveProduct({ slug: form.slug, name: form.name, description: form.description, basePrice: Number(form.basePrice), salePrice: form.salePrice ? Number(form.salePrice) : null, type: "PRODUCT", inventory: Number(form.inventory), featured: editing?.featured || false, categoryId: form.categoryId, status: form.status, images: [form.image], setupStatus: form.setupStatus, careLevel: form.careLevel, chatTags: [], displayReviewCount: editing?.reviewCount || 0, variants: [], careGuide: { watering: form.watering, sunlight: "Ánh sáng gián tiếp", temperature: "20-28°C", humidity: "Duy trì ẩm", commonIssues: "Tránh úng và nắng trực tiếp" } }, editing?.id); setOpen(false); await load(); } catch (error) { setMessage(getApiMessage(error)); } } return <><AdminHead title="Sản phẩm" description="CRUD catalog, tồn kho, giá sale và hướng dẫn chăm sóc." action={<button onClick={() => start()}><Plus /> Thêm sản phẩm</button>} />{message && <p className="notice error">{message}</p>}{!products ? <Loading /> : <div className="admin-table-wrap"><table><thead><tr><th>Sản phẩm</th><th>Danh mục</th><th>Giá</th><th>Tồn</th><th>Trạng thái</th><th /></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td><div className="table-product"><img src={product.images[0] || "/logo.png"} alt="" /><div><b>{product.name}</b><span>{product.slug}</span></div></div></td><td>{product.categoryName}</td><td>{formatMoney(product.effectivePrice)}</td><td>{product.inventory}</td><td><span className={`status ${product.status.toLowerCase()}`}>{product.status}</span></td><td><button className="icon-button" onClick={() => start(product)}><Edit3 /></button><button className="icon-button danger" onClick={async () => { if (confirm("Lưu trữ sản phẩm này?")) { await deleteProduct(product.id); await load(); } }}><Trash2 /></button></td></tr>)}</tbody></table></div>}{open && <div className="modal-backdrop"><form className="modal panel" onSubmit={submit}><div className="modal-head"><h2>{editing ? "Sửa sản phẩm" : "Thêm sản phẩm"}</h2><button type="button" className="ghost" onClick={() => setOpen(false)}>✕</button></div><div className="form-grid"><label>Tên<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><label>Slug<input required value={form.slug} onChange={(event) => setForm({ ...form, slug: event.target.value })} /></label><label>Giá gốc<input required type="number" value={form.basePrice} onChange={(event) => setForm({ ...form, basePrice: Number(event.target.value) })} /></label><label>Giá sale<input type="number" value={form.salePrice} onChange={(event) => setForm({ ...form, salePrice: event.target.value })} /></label><label>Tồn kho<input required type="number" value={form.inventory} onChange={(event) => setForm({ ...form, inventory: Number(event.target.value) })} /></label><label>Danh mục<select required value={form.categoryId} onChange={(event) => setForm({ ...form, categoryId: event.target.value })}>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Trạng thái<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })}><option>ACTIVE</option><option>DRAFT</option><option>OUT_OF_STOCK</option><option>ARCHIVED</option></select></label><label>Mức chăm<select value={form.careLevel} onChange={(event) => setForm({ ...form, careLevel: event.target.value })}><option>EASY</option><option>MEDIUM</option><option>ADVANCED</option></select></label><label className="full">Mô tả<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label><label className="full">Ảnh URL<input value={form.image} onChange={(event) => setForm({ ...form, image: event.target.value })} /></label><label className="full">Lịch tưới<input value={form.watering} onChange={(event) => setForm({ ...form, watering: event.target.value })} /></label></div><button className="wide">Lưu sản phẩm</button></form></div>}</>; }
